<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base: pantalla completa, sin action bar, fondo negro -->
    <style name="Theme.APiptv" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorPrimary">#3b6dff</item>
        <item name="android:colorAccent">#3b6dff</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:hardwareAccelerated">true</item>
    </style>

    <!-- Tema para TV / FireTV: leanback, sin touch, cursor resaltado -->
    <style name="Theme.APiptv.Tv" parent="@style/Theme.Leanback">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorPrimary">#3b6dff</item>
        <item name="android:colorAccent">#3b6dff</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:hardwareAccelerated">true</item>
    </style>

</resources>
